PHP API STRUCTURE
~~~
api/index.php
	 ?create={id}&{columns/values}
	 ?id={id}
	 ?{fields}&where={criteria}&condition={OR/AND}&order={columns}&sort={asc/desc}
	 ?update={id}&{columns/values}
	 ?update={columns/values}&where={criteria}&condition={OR/AND}
	 ?delete={id}
~~~
